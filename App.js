import React from 'react';
import { enableScreens } from 'react-native-screens';
enableScreens();

import { Block, Text } from 'galio-framework';
import NotifikasiApp from './src/landing';

const App = ({}) => {
  
      return (
        	<NotifikasiApp />
      );  
    
}

export default App;